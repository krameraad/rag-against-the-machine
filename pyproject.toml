[project]
name = "rag-against-the-machine"
version = "0.1.0"
description = "Will you answer my questions?"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fire>=0.7.1",
    "pydantic>=2.13.5",
    "tqdm>=4.70.0",
]
